{
  "name": "ui-system-repo",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "typecheck": "pnpm run -r typecheck",
    "lint": "pnpm run -r lint",
    "lint:fix": "pnpm run -r lint:fix"
  }
}
